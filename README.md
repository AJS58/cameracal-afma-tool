Cameracal AFMA Report Tool v26

Fix:
- Corrects AF menu overlay placement so it sits inside the black LCD only.
- Prevents the menu overlapping the camera body.
- Applies same smaller LCD overlay placement to the PDF/print report.
- Keeps working controls and report generation.
- Uses app-v26.js to avoid cache issues.

Upload all unzipped files to GitHub root and commit changes.
