Cameracal Canon AF Microadjustment Report Tool v32

Update:
- PDF report now duplicates the same Canon AFMA menu component used on the front-end.
- This removes the separate simplified PDF-only AFMA rendering.
- Live app and PDF now share the same menu structure, styling, W/T values and lens description.
- Rear camera body image remains excluded.
- Separate Wide / Tele subject distance dropdowns in metres remain included.
- Uses app-v32.js to avoid browser cache issues.

Upload all unzipped files to the GitHub repository root and commit changes.
